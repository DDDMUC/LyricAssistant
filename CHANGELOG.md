# 更新日志

## v0.6.0

这次更新很大：新增 **AI 填词**面板、**MIDI 导入 / 导出 / 拖拽**、**词格酱 MIDI 兼容**、**网页版**，以及一批输入与界面改进。

### AI 填词（全新，桌面版）

- 工具栏「AI 填词」打开**右侧聊天面板**（与「原文」互斥、宽度可拖、开关状态记住）
- **内置 DeepSeek 和小米 MiMo**（地址、模型都已配好，填 Key 即用；另有「自定义」接口）：输入框下方点**模型名**弹出带搜索的模型选择器；推理等级 Default / None / Low / High / Max（按模型实际能力显示）
- ⚙ 设置：逐家管地址 / 模型 / Key / 测试连接 / 温度 / **输出上限**（默认「不限制」，可选「自动」按词格估一个保险丝额度）；自定义接口可勾选是否支持 thinking / 推理等级（默认都不发）；**Key 只存本机**
- 每次请求自动把词格打包给模型：分组字数、锁定辙（附韵母表）、已有字、和声、备注；范围可选 整首 / 只填空句 / 当前段 / 选中的句子；没有可写的句子时按**纯聊天**发（不附词格）
- 回复**流式**显示；思维链默认**折叠**成「✦ 已思考（用时 N 秒）›」，展开整段铺开、不再是小窗；回复**按歌词行渲染**（段落小标题 + 句序号、没过的句子标红、**每句按词格分组断开**）；真被额度截断会标「⚠ 输出被额度截断」
- 本地逐句硬校验（字数 / 只许汉字 / 锁定辙合辙），不合格**自动回喂修正最多 3 次**；「填入词格」= 空格子直填、有字的句子自动进「**AI**」备选、整次一步撤销
- 回复支持**多版本**：⟳ 原地再生成一版（不往对话里追加一轮），气泡里用 `‹ 2/2 ›` 在版本间翻；每版各带自己的思维链、校验脚注和「填入词格 / 复制」
- 面板头「**提示词**」：随时查看真正发出去的系统提示词与本次请求内容

### 网页版（新）

- **同一份代码现在也能在浏览器里用**（Safari / Chrome / Edge）：编辑、自动保存、多文件、打开工程、导入（txt / lrc / md / MIDI）、导出（歌词 / 词格 / 带歌词 MIDI）、拖拽导入全部可用；**桌面版行为不变**
  - 保存：Chrome / Edge 走「**就地覆盖**」（第一次选好位置，之后 `Cmd/Ctrl+S` 直接覆盖；刷新页面后需重选一次）；Safari 没有这个能力，退回**下载**（每次保存 = 下载一份新文件）
  - 「AI 填词」仅桌面版提供：网页版按钮置灰，提示「AI 填词仅桌面版可用」
  - 拖拽在浏览器里读的是文件本身（拿不到磁盘路径），导入体验与桌面一致
  - 构建产物改用相对路径，可以直接挂在静态服务器的任意子目录下（如 GitHub Pages 项目页）；本地用 `npm run dev` / `npm run preview` 即可打开

### MIDI（新）

- **导入 MIDI 生成词格**：「导入 → 选择 MIDI…」——选轨道、选「分段依据」、调「换句休止」阈值（或按 Marker 分段），实时预览段 / 句 / 格数后导入；一个音符一格，文件里带 Lyric 事件的话字也一起填进来
- **导出带歌词 MIDI**：「导出 → 带歌词 MIDI」把当前词格的字写回音符的 Lyric 事件（空格子写空）；导出前逐句核对词格和导入时是否还对得上，对不上会报「第几段第几句」并拦下
- **拖拽导入**：把 `.mid / .txt / .lrc / .md / .json` 拖进窗口就能读（松手时窗口有提示）
- **词格酱兼容**：导入窗「分段依据」三选一——**自动**（这条轨里出现 C0/C#0/D0 就按它的规则切，没有就照旧走休止 + Marker）/ **休止 + Marker**（强制用我们的）/ **词格酱标记**（强制用它）；规则照它的规范：C0 分句、C#0 换句、D0 换段，两个标记之间没音符不生成空组/空句/空段，只认选中的那条轨（界面会提示 C0/C#0/D0 是「Middle C=C3」那种 DAW 显示、实际编号是 24/25/26）
  - **导出自动跳过标记音**（不把它们当要唱歌的音，歌词不会整体错位一格）；这次导入用过标记的话，导出前会多问一句「要不要去掉切分标记音」——不勾就原样保留（还能拿回词格酱/DAW 继续用），勾上等于它的「导出纯净 MIDI」

### 输入与编辑

- **只收汉字**：歌词格子只收汉字，导入、打字、粘贴里夹带的英文 / 拼音 / 数字一律**跳过、不占格**（`空天裂降（jiàng）三军` → `空天裂降三军`；整行都是拼音的不会生成句子）
- 「+ 新增一句」移到**每一句的右下角**（点它在这一句下面插新句），不再只在段落末尾有一个

### 界面与修复

- 窗口右侧的滚动条藏掉，换成**内容区顶部的 2px 进度条**（贴在工具栏下沿；只动窗口这一条，侧栏、AI 面板、原文面板内部的滚动条不受影响）
- 滚轮**只管鼠标底下那一个滚动区**：AI 对话 / 文档列表 / 原文面板 / 弹窗 / 下拉弹层滚到底后，不再带动主页面一起滚
- **弹窗不再叠罗汉**：拖 MIDI 进窗口（或从「导入」里选 MIDI）时，会先把还开着的旧弹窗收掉——之前 MIDI 弹窗会压在「导入歌词」上面，导入完还得再点一次「导入」才能把那个旧弹窗打发掉
- 窗口标题后缀改为「· 作词助手」

## v0.5.0

### 更名

- 产品定名「**作词助手**（Lyric Assistant）」：App 显示名、窗口标题、`package.json`、README 与 GitHub 仓库（`DDDMUC/LyricAssistant`）同步；内部标识改为 `com.mu.lyricassistant`

### 新功能

- 工具栏右侧新增「清空」：一键清空所有格子（保留歌名、备注；每句已认出的辙会被记住，清空后辙信息不丢、但不上锁），可撤销
- 单句的「清空」同样记住该句的韵辙
- **和声句**：整行括号的歌词（如 `（煌煌国运华盖将倾 分说不尽且待星移）`）导入时不再被丢弃，而是生成紧跟主句的「和声句」——有自己的词格、可编辑、导出时还原成带括号的一行；也可手动「+ 和声」新增，或用句内「和声」按钮把某句标记/取消
- 「导出歌词」「复制歌词」改为「**导出**」「**复制**」下拉菜单：各自可选「歌词 / 词格」；导出词格/复制词格只包含词格结构（每格用 `X`、段落名写成 `[名]`、和声句带括号），能被「导入歌词」原样读回
- 导出 / 复制歌词按词格对齐：一格一字，空格子用 `X` 占位（单句「复制句」同样）
- 按住左键拖动可**框选一串格子（可跨句）**（高亮）：`Backspace / Delete` 清空选中格，`Cmd/Ctrl+C` 复制选中的字（跨句按行输出），`Esc` 或单击取消

### 导入修复

- 导入时 `X` 也当空格子（与导出/复制的 `X` 占位对齐，「导出 → 再导入」完全一致）
- 补全创作信息署名词：「调教 / 曲绘 / 动画 / 视频 / 分镜 / 字幕 / PV / 古琴 / 音效」不再被当成歌词行导进正文

### 韵辙锁定

- 锁定时**只允许输入汉字**：字母、数字、标点一律拦下并提醒（未锁时无限制）

### 编辑体验修复

- 修复「删光某个字的拼音后连打删除键，把前一个字一起带走」：组字刚结束的第一次 Backspace 原地不动，绝不触碰前一格
- 空格 Backspace 仍可删掉前一格的字并左移，不会出现光标完全无法回退的情况

## v0.4.0

### 韵辙锁定（新功能）

- 每句徽章常驻可点：未锁时只显示辙名，已锁显示「江阳 🔒」，句尾为空时显示「＋ 锁」
- 锁定后，**句尾最后一格**只接受押该辙的字：按全部读音判定（多音字任一读音命中即放行），不中则整笔拦下并红字提醒
- 已锁但句尾不合辙 → 徽章变红警示；挪句 / 备选切换带入的非韵字同样只警示不拦
- 锁定写进工程文件，可撤销；支持 j/q/x/y 后 ü 的还原（qu→v、xue→ve）
- 锁定弹窗提供 13 辙选择与「解锁」

### 备份

- 状态栏的文件状态（「未保存到文件」/ 文件路径）可点：点一下 = 保存草稿备份，把全部歌词文件导出成一个 `.json`（自选位置）
- 备份格式：`{ format: "cige-grid-drafts", version: 1, exportedAt, activeId, docs }`，`docs` 内项目与工程 `.json` 同构
- 「打开工程」支持直接打开草稿备份：自动识别格式并整体恢复全部歌词文件
- 自动保存只写本机数据库，不生成任何文件
- README 增加「数据与备份」，说明本机存储键位与格式

### 界面

- 「导入原文（核对用）」面板改为**悬浮固定**：主页面上下滚时它钉在右侧不动，原文在面板内独立滚动
- 状态栏改为整宽，与标题栏同级：不再被左侧栏切分，左侧栏夹在标题栏与状态栏之间
- 段落左侧发丝线改为固定像素渐隐（上下各 24px）：长短段落两端都收得干净
- 韵脚徽章去掉 🔓，只在锁定时显示 🔒（淡色 = 未锁 / 实色 = 已锁）
- 锁定弹窗的「不锁定」改名「解锁」

### 修复

- 修复所有弹窗（导入 / 删除 / 韵辙 / 创作信息 / 草稿备份）打开时把页面拉到顶部
- 修复弹窗不垂直居中：`dialog { position: relative }` 覆盖了浏览器默认的 `position: fixed` 居中规则
- 修复点弹窗按钮后页面跳回顶部：`render()` 的 `replaceChildren()` 先清空容器导致 scrollTop 被钳到 0，现保存并恢复滚动位置
- 修复「导入原文」面板随主页面滚动而消失

## v0.3.0

### 溢出处理

- 每句可暂存超出词格的字：写入、粘贴、删格、缩小词格时多出的字自动收集为「溢出」
- 句子右侧以红色「＋…」标注，状态栏显示「溢出 N 字」，顶栏出现「整理溢出」按钮
- 「整理溢出」把每句溢出顺移到下一句开头；末句溢出自动新建一句
- 导出歌词时，溢出字附在该句末尾

### 多歌词文件

- 现在可以删除全部歌词文件：工作区允许为空，显示「还没有歌词」空状态
- 空工作区下仅保留「导入歌词 / 打开工程」；新句、保存、导出、撤销等需要工程的按钮自动隐藏（Cmd+S 也有保护）
- 空状态导入歌词时会自动创建第一个歌词文件，内容不会丢
- 首次启动仍会自动建一份示例；删除到 0 之后再启动也保持空状态，不再自动恢复
- 左侧新增「歌词文件」边栏：新建、切换、删除多个文件，自动保存到本地，重启恢复
- 「打开工程」改为在边栏新增一个文件，不再覆盖当前文件
- 侧边栏宽度可拖动调整（140–420px，双击恢复默认，宽度本地记忆）
- 侧边栏文件支持双击标题重命名（Enter 保存 / Esc 取消）
- 侧边栏收起 / 展开按钮位于侧边栏右上角，收起后从正文顶栏展开

### 界面

- 换用稿纸配色：暖纸底色、暖橙红强调色、格子内文字改楷体
- 句子改为「左侧序号 + 词格，中间格子，右侧韵脚与进度」布局
- 控制行（备选/备注/复制句等）移到格子上方，固定占位，仅悬停或选中时显形
- 句子之间改用虚线分隔；导入弹窗右上角新增 × 关闭
- 备选支持重命名（✎，Enter 保存 / Esc 取消）
- 句子控制行新增「清空」：一键清空该句当前备选的所有字（可撤销）
- 句子控制行新增 `／`（光标处断句 / 与上一分句合并）与 `⧉`（复制本句词格插入新句）
- 快捷键 `⌘[` / `⌘]` 减 / 加格，`⌘\` 断句合并；`−` 在分组只剩一格时连同分组一起删
- 「导出歌词」（.txt）改为主按钮，「保存工程」（.json）语义明确
- 导出前可选：带上全部备选（`※` 分隔）、备注与创作信息；导出的 TXT 可原样再导入，勾选状态本地记忆
- 句子控制按钮顺序调整为「`− + ／ ← → ⧉ 删句` + 复制句 / 粘贴句 / 清空」

### 导入

- 歌词文本导入可直接填字；新增「仅词格 / 同时导入歌词」两种模式
- 支持《标题》、`[段落名]`、行尾（备注）、`|` / `※` 分隔备选、纯数字词格行、LRC 时间戳剥离
- 段落关键词行自动识别为段落名：Verse / Chorus / 主歌 / 副歌 等（可带数字、括号、冒号，如 `chorus 2`、`【副歌】`、`副歌：`）
- `XXXX` / `XX` 占位行只生成词格、不填字，空句可原样回读
- 既无空行也无段落标记时，按每 4 句自动分段（余 1 句并入上一段）
- 未命名段落自动命名为「段落 N」
- 创作信息行（作词 / 作曲 / 编曲 / 混音 / 母带 / 制作人 / 监制 / 发行…，含一行多条与英文标签）自动提取，不生成词格
- 顶栏「创作信息」可查看与编辑；导出歌词可选写在 TXT 顶部，导入时原样读回
- credit 块中夹着的短裸行（如歌名「人间应又雪」，下一行是 credit）识别为标题
- 覆盖式导入会整体替换创作信息：新内容没有 credit 时清空；合并导入仍去重追加
- 歌名也按元数据同样处理：覆盖导入时识别到就替换，没有就清空回「未命名歌曲」（合并导入不变）
- 新增「导入原文」面板：导入的原文不再丢弃，自动保留在右侧面板里，可逐句核对（合并导入则依次追加），顶栏「原文」按钮随时开关
- 补充乐器 / 分轨类 credit 词：二胡、琵琶、古筝、小提琴、分轨混音/母带 等

### 其它

- 新增 `parseLyrics` 解析器与单元测试

## v0.2.0

### 新增

- 韵脚提示：自动识别每句句尾的十三辙韵部，同韵同色徽章；句尾未填时灰显、不计入统计
- 状态栏韵脚统计，按辙汇总句数
- 段落头新增「+ 段」按钮：在当前段落之后插入新段落
- 段落左侧彩色竖条，标题按段落配色

### 改进

- 编辑格直接显示原字，光标默认停在字右侧
- `Backspace` 在已填格上原地删字，可连按连续删除；`Delete` 同效
- 按钮改为无边框 ghost 风格，主操作（保存 / 导入）为实心主按钮
- 输入框改为填充式样式，聚焦时蓝框光晕
- 去掉句子卡片白底，格子直接置于页面背景；格子描边细化
- 移除顶栏「新段落」按钮（由段落头「+ 段」替代）

### 修复

- 修复 `Backspace` / `Delete` 连续删除失效（清格后焦点丢失）
- 修复标题栏拖拽缺少权限（`core:window:allow-start-dragging`）

### 依赖

- 新增 pinyin-pro（MIT）用于拼音韵母识别

## v0.1.0

- 初始开源版本：钉子格编辑、句内分组、备选与备注、段落管理、撤销/重做、自动保存、导入导出、剪贴板、浅色/深色主题
