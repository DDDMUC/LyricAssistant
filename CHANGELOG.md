# 更新日志

## v0.4.0

### 韵辙锁定（新功能）

- 每句徽章常驻可点：未锁时只显示辙名，已锁显示「江阳 🔒」，句尾为空时显示「＋ 锁」
- 锁定后，**句尾最后一格**只接受押该辙的字：按全部读音判定（多音字任一读音命中即放行），不中则整笔拦下并红字提醒
- 已锁但句尾不合辙 → 徽章变红警示；挪句 / 备选切换带入的非韵字同样只警示不拦
- 锁定写进工程文件，可撤销；支持 j/q/x/y 后 ü 的还原（qu→v、xue→ve）
- 锁定弹窗提供 13 辙选择与「解锁」

### 备份

- 状态栏的文件状态（「未保存到文件」/ 文件路径）可点：点一下 = 保存草稿备份，把全部歌词文件导出成一个 `.json`（自选位置）
- 备份格式：`{ format: "cige-grid-drafts", version: 1, exportedAt, activeId, docs }`，`docs` 内项目与工程 `.json` 同构
- 「打开工程」支持直接打开草稿备份：自动识别格式并整体恢复全部歌词文件
- 自动保存只写本机数据库，不生成任何文件
- README 增加「数据与备份」，说明本机存储键位与格式

### 界面

- 「导入原文（核对用）」面板改为**悬浮固定**：主页面上下滚时它钉在右侧不动，原文在面板内独立滚动
- 状态栏改为整宽，与标题栏同级：不再被左侧栏切分，左侧栏夹在标题栏与状态栏之间
- 段落左侧发丝线改为固定像素渐隐（上下各 24px）：长短段落两端都收得干净
- 韵脚徽章去掉 🔓，只在锁定时显示 🔒（淡色 = 未锁 / 实色 = 已锁）
- 锁定弹窗的「不锁定」改名「解锁」

### 修复

- 修复所有弹窗（导入 / 删除 / 韵辙 / 创作信息 / 草稿备份）打开时把页面拉到顶部
- 修复弹窗不垂直居中：`dialog { position: relative }` 覆盖了浏览器默认的 `position: fixed` 居中规则
- 修复点弹窗按钮后页面跳回顶部：`render()` 的 `replaceChildren()` 先清空容器导致 scrollTop 被钳到 0，现保存并恢复滚动位置
- 修复「导入原文」面板随主页面滚动而消失

## v0.3.0

### 溢出处理

- 每句可暂存超出词格的字：写入、粘贴、删格、缩小词格时多出的字自动收集为「溢出」
- 句子右侧以红色「＋…」标注，状态栏显示「溢出 N 字」，顶栏出现「整理溢出」按钮
- 「整理溢出」把每句溢出顺移到下一句开头；末句溢出自动新建一句
- 导出歌词时，溢出字附在该句末尾

### 多歌词文件

- 现在可以删除全部歌词文件：工作区允许为空，显示「还没有歌词」空状态
- 空工作区下仅保留「导入歌词 / 打开工程」；新句、保存、导出、撤销等需要工程的按钮自动隐藏（Cmd+S 也有保护）
- 空状态导入歌词时会自动创建第一个歌词文件，内容不会丢
- 首次启动仍会自动建一份示例；删除到 0 之后再启动也保持空状态，不再自动恢复
- 左侧新增「歌词文件」边栏：新建、切换、删除多个文件，自动保存到本地，重启恢复
- 「打开工程」改为在边栏新增一个文件，不再覆盖当前文件
- 侧边栏宽度可拖动调整（140–420px，双击恢复默认，宽度本地记忆）
- 侧边栏文件支持双击标题重命名（Enter 保存 / Esc 取消）
- 侧边栏收起 / 展开按钮位于侧边栏右上角，收起后从正文顶栏展开

### 界面

- 换用稿纸配色：暖纸底色、暖橙红强调色、格子内文字改楷体
- 句子改为「左侧序号 + 词格，中间格子，右侧韵脚与进度」布局
- 控制行（备选/备注/复制句等）移到格子上方，固定占位，仅悬停或选中时显形
- 句子之间改用虚线分隔；导入弹窗右上角新增 × 关闭
- 备选支持重命名（✎，Enter 保存 / Esc 取消）
- 句子控制行新增「清空」：一键清空该句当前备选的所有字（可撤销）
- 句子控制行新增 `／`（光标处断句 / 与上一分句合并）与 `⧉`（复制本句词格插入新句）
- 快捷键 `⌘[` / `⌘]` 减 / 加格，`⌘\` 断句合并；`−` 在分组只剩一格时连同分组一起删
- 「导出歌词」（.txt）改为主按钮，「保存工程」（.json）语义明确
- 导出前可选：带上全部备选（`※` 分隔）、备注与创作信息；导出的 TXT 可原样再导入，勾选状态本地记忆
- 句子控制按钮顺序调整为「`− + ／ ← → ⧉ 删句` + 复制句 / 粘贴句 / 清空」

### 导入

- 歌词文本导入可直接填字；新增「仅词格 / 同时导入歌词」两种模式
- 支持《标题》、`[段落名]`、行尾（备注）、`|` / `※` 分隔备选、纯数字词格行、LRC 时间戳剥离
- 段落关键词行自动识别为段落名：Verse / Chorus / 主歌 / 副歌 等（可带数字、括号、冒号，如 `chorus 2`、`【副歌】`、`副歌：`）
- `XXXX` / `XX` 占位行只生成词格、不填字，空句可原样回读
- 既无空行也无段落标记时，按每 4 句自动分段（余 1 句并入上一段）
- 未命名段落自动命名为「段落 N」
- 创作信息行（作词 / 作曲 / 编曲 / 混音 / 母带 / 制作人 / 监制 / 发行…，含一行多条与英文标签）自动提取，不生成词格
- 顶栏「创作信息」可查看与编辑；导出歌词可选写在 TXT 顶部，导入时原样读回
- credit 块中夹着的短裸行（如歌名「人间应又雪」，下一行是 credit）识别为标题
- 覆盖式导入会整体替换创作信息：新内容没有 credit 时清空；合并导入仍去重追加
- 歌名也按元数据同样处理：覆盖导入时识别到就替换，没有就清空回「未命名歌曲」（合并导入不变）
- 新增「导入原文」面板：导入的原文不再丢弃，自动保留在右侧面板里，可逐句核对（合并导入则依次追加），顶栏「原文」按钮随时开关
- 补充乐器 / 分轨类 credit 词：二胡、琵琶、古筝、小提琴、分轨混音/母带 等

### 其它

- 新增 `parseLyrics` 解析器与单元测试

## v0.2.0

### 新增

- 韵脚提示：自动识别每句句尾的十三辙韵部，同韵同色徽章；句尾未填时灰显、不计入统计
- 状态栏韵脚统计，按辙汇总句数
- 段落头新增「+ 段」按钮：在当前段落之后插入新段落
- 段落左侧彩色竖条，标题按段落配色

### 改进

- 编辑格直接显示原字，光标默认停在字右侧
- `Backspace` 在已填格上原地删字，可连按连续删除；`Delete` 同效
- 按钮改为无边框 ghost 风格，主操作（保存 / 导入）为实心主按钮
- 输入框改为填充式样式，聚焦时蓝框光晕
- 去掉句子卡片白底，格子直接置于页面背景；格子描边细化
- 移除顶栏「新段落」按钮（由段落头「+ 段」替代）

### 修复

- 修复 `Backspace` / `Delete` 连续删除失效（清格后焦点丢失）
- 修复标题栏拖拽缺少权限（`core:window:allow-start-dragging`）

### 依赖

- 新增 pinyin-pro（MIT）用于拼音韵母识别

## v0.1.0

- 初始开源版本：钉子格编辑、句内分组、备选与备注、段落管理、撤销/重做、自动保存、导入导出、剪贴板、浅色/深色主题
